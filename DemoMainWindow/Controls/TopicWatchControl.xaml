<UserControl x:Class="DemoMainWindow.TopicWatchControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800">
	<DataGrid ItemsSource="{Binding TopicWatches}"
			  AutoGenerateColumns="False"
			  CanUserAddRows="False"
			  SelectionMode="Single"
			  Margin="10"
			  materialDesign:DataGridAssist.CellPadding="8"
			  materialDesign:DataGridAssist.ColumnHeaderPadding="8">
		<DataGrid.Columns>
			<DataGridTextColumn Header="Group ID" Binding="{Binding GroupId}" Width="*"/>
			<DataGridTextColumn Header="Topic" Binding="{Binding Topic}" Width="*"/>
			<DataGridTextColumn Header="Progress" Binding="{Binding Progress}" Width="120"/>
			<DataGridTemplateColumn Header="Progress %" Width="200">
				<DataGridTemplateColumn.CellTemplate>
					<DataTemplate>
						<StackPanel>
							<TextBlock Text="{Binding ProgressPercentage, StringFormat={}{0:F1}%, Mode=OneWay}"
									   HorizontalAlignment="Center"
									   Margin="0,0,0,5"/>
							<ProgressBar Value="{Binding ProgressPercentage, Mode=OneWay}"
										 Maximum="100"
										 Height="20"/>
						</StackPanel>
					</DataTemplate>
				</DataGridTemplateColumn.CellTemplate>
			</DataGridTemplateColumn>
		</DataGrid.Columns>
	</DataGrid>
</UserControl>
